name: criar hello world
on:
  issues:
    types: [opened, reopened]

jobs:
  issue-to-json:
    name: Convert issue body to json
    if: contains(github.event.issue.body, '>>linguagem<<')
    runs-on: ubuntu-latest
    outputs:
      payload-parsed: ${{ steps.payload.outputs.json_var }}
    steps:
      - name: Run parser
        id: parse
        uses: peter-murray/issue-forms-body-parser@v2.0.0
        with:
          issue_id: ${{ github.event.issue.number }}
          separator: '###'
          label_marker_start: '>>'
          label_marker_end: '<<'
      - name: Set Output
        id: payload
        run: echo json_var='${{ steps.parse.outputs.payload }}' >> $GITHUB_OUTPUT

  execute-pipe:
    name: criar hello world
    if: contains(github.event.issue.body, '>>linguagem<<')
    runs-on: ubuntu-latest
    needs: issue-to-json
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
     
      - name: 'code add'
        id: status
        continue-on-error: true
        uses: google/gemini-pro-action@v1
        # env:
        #   linguagem: ${{ fromJson(needs.issue-to-json.outputs.payload-parsed)['linguagem'] }}
        #   extra: ${{ fromJson(needs.issue-to-json.outputs.payload-parsed)['funcionalidade_extra'] }
        with:
          prompt: ${{ fromJson(needs.issue-to-json.outputs.payload-parsed)['linguagem'] }}
          output_file: poem.txt

      - name: "Commit changes"
        uses: EndBug/add-and-commit@v9
        if: steps.status.outcome == 'success'
        with:
          message: new code
          committer_name: GitHub Actions
          committer_email: actions@github.com

      - name: 'Close issue: code added!'
        uses: peter-evans/close-issue@v2
        if: steps.status.outcome == 'success'
        with:
          comment: |
            Closing issue with success status
            Reason: code added
          labels: |
            code_added
  